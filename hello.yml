config:
  target: 'http://localhost:3003'
  http:
    pool: 100000
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "get"
    flow:
    # - loop: 
      - get:
          url: "/reviews/{{ $randomNumber(999900, 1000000) }}"
    count: 10
  - name: "post"
    flow: 
      # - loop: 
        - post:
            url: "/newreview"
    count: 10